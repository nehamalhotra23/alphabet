export function Alphabet(letter) {
  this.letter = letter;
}

Alphabet.prototype.naMes = function(letter) {
 
    // let regex = /[a-z]/g;
    // return this.letter.length;
      // return this.letter.length;
  };
